using UnityEngine;

public class NetWorkClintSend : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }//Nota não dei pq isso existe

    // Update is called once per frame
    void Update()
    {
        
    }
}
